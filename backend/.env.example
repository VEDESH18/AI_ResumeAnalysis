# Server
PORT=4000
# Comma-separated list of allowed origins for CORS
CORS_ORIGINS=http://localhost:4000,http://localhost:3000,http://localhost:5173

# Database (optional; if set, Postgres is used instead of JSON storage)
# Example for Render/Neon/Supabase (Postgres):
# DATABASE_URL=postgres://USERNAME:PASSWORD@HOST:5432/DBNAME?sslmode=require
DATABASE_URL=

# MongoDB Atlas (recommended). Paste your connection string here.
# Get it from Atlas: Database > Connect > Drivers > "Node.js" connection string
# Example:
# MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER-ID.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=
# Optional DB name (defaults to ai_resume_analyzer)
MONGODB_DB=

# Optional integrations (not required for basic features)
# OpenAI API for advanced suggestions
OPENAI_API_KEY=

# LanguageTool API for grammar checks
# Public endpoint (no key, rate-limited):
LANGUAGETOOL_API_URL=https://api.languagetool.org/v2
# For premium cloud accounts, set key/username if applicable:
LANGUAGETOOL_API_KEY=
LANGUAGETOOL_USERNAME=

# Clerk Authentication (optional)
# Get keys from https://dashboard.clerk.com/ > Your application > API Keys
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
